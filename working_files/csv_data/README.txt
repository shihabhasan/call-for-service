The CSV data files are too large to store in the repo, so they're in the cfs_dpd directory in the public share folder on Moxie.

Go there, grab csv_data.zip, and extract it into this directory if you need the data to run the ETL script.
